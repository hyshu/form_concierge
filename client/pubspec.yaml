name: form_concierge_client
description: REST client for Form Concierge Workers API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.2
  web: ^1.1.1

dev_dependencies:
  test: ^1.31.1
