name: form_concierge_survey_widget
description: Embeddable survey widget for form_concierge
version: 0.1.0

environment:
  sdk: ^3.8.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  form_concierge_client:
    path: ../client
  serverpod_auth_core_flutter: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
