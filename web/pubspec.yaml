name: form_concierge_web
description: Web survey form using Jaspr

publish_to: none

environment:
  sdk: ^3.6.0

jaspr:
  mode: server

dependencies:
  jaspr: ^0.23.1
  jaspr_router: ^0.8.2
  form_concierge_client:
    path: ../client
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.15.0
  build_web_compilers: ^4.8.0
  jaspr_builder: ^0.23.1
  lints: ^6.1.0
